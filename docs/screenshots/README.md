# Screenshots

Drop the app screenshots here with these exact filenames so the root README picks them up:

- `home.png`
- `friends.png`
- `findtime.png`
- `profile.png`

Recommended: portrait phone frames, ~1080px wide, PNG. After adding them,
uncomment the screenshot table in the root [`README.md`](../../README.md).
